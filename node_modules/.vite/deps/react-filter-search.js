import {
  require_prop_types
} from "./chunk-5OZKBLEG.js";
import {
  __commonJS,
  __toESM,
  require_react
} from "./chunk-S2TLTWWO.js";

// node_modules/@strikeentco/get/main.js
var require_main = __commonJS({
  "node_modules/@strikeentco/get/main.js"(exports, module) {
    "use strict";
    var isObject = (val) => Object.prototype.toString.call(val) === "[object Object]";
    var get = (obj, parts, length) => {
      for (let i = 0; i < length; i++) {
        if (obj === null) {
          return;
        }
        const v = obj[parts[i]];
        if (v === void 0) {
          return;
        }
        obj = v;
      }
      return obj;
    };
    module.exports = (obj, path, sep = ".") => {
      if (!isObject(obj) || !path) {
        return obj;
      }
      const parts = Array.isArray(path) ? path : String(path).split(sep);
      const { length } = parts;
      return length < 2 ? obj[parts[0]] : get(obj, parts, length);
    };
  }
});

// node_modules/@strikeentco/set/main.js
var require_main2 = __commonJS({
  "node_modules/@strikeentco/set/main.js"(exports, module) {
    "use strict";
    var isObject = (val) => typeof val === "object" || typeof val === "function";
    var isProto = (val, obj) => val == "__proto__" || val == "constructor" && typeof obj.constructor === "function";
    var set = (obj, parts, length, val) => {
      let tmp = obj;
      let i = 0;
      for (; i < length - 1; i++) {
        const part = parts[i];
        if (isProto(part, tmp)) {
          continue;
        }
        tmp = !isObject(tmp[part]) ? tmp[part] = {} : tmp[part];
      }
      tmp[parts[i]] = val;
      return obj;
    };
    module.exports = (obj, path, val, sep = ".") => {
      if (!isObject(obj) || !path || !path.length) {
        return obj;
      }
      const parts = Array.isArray(path) ? path : String(path).split(sep);
      if (isProto(parts[0], obj)) {
        return obj;
      }
      const { length } = parts;
      if (length === 1) {
        obj[parts[0]] = val;
        return obj;
      }
      return set(obj, parts, length, val);
    };
  }
});

// node_modules/pick-deep/main.js
var require_main3 = __commonJS({
  "node_modules/pick-deep/main.js"(exports, module) {
    "use strict";
    var get = require_main();
    var set = require_main2();
    var isObject = (val) => Object.prototype.toString.call(val) === "[object Object]";
    var pick2 = (obj, paths, length, sep) => {
      const picked = {};
      for (let i = 0; i < length; i++) {
        const path = paths[i];
        const val = get(obj, path, sep);
        if (val === void 0) {
          continue;
        }
        set(picked, path, val, sep);
      }
      return picked;
    };
    module.exports = (obj, paths, sep = ".") => {
      if (!isObject(obj) || !paths || !(Array.isArray(paths) || typeof paths === "string")) {
        return {};
      }
      const { length } = paths;
      if (typeof paths === "string" || length < 2) {
        const path = typeof paths === "string" ? paths : paths[0];
        const val = get(obj, path, sep);
        return val !== void 0 ? set({}, path, val, sep) : {};
      }
      return pick2(obj, paths, length, sep);
    };
  }
});

// node_modules/react-filter-search/es/index.js
var import_react = __toESM(require_react());
var import_prop_types = __toESM(require_prop_types());

// node_modules/react-filter-search/es/filter.js
var import_pick_deep = __toESM(require_main3());
var _typeof = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? function(obj) {
  return typeof obj;
} : function(obj) {
  return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj;
};
function strOp(str) {
  return str.toString().replace(/\s/g, "").toLowerCase();
}
function objectValues(value, pickAttr) {
  return (pickAttr ? Object.values((0, import_pick_deep.default)(value, pickAttr)) : Object.values(value)).reduce(function(string, val) {
    var test = val !== null && val !== void 0;
    return string + ((typeof val === "undefined" ? "undefined" : _typeof(val)) === "object" && val !== null ? strOp(objectValues(val)) : test ? strOp(val) : "");
  }, "");
}
function filter(val, data, pick2) {
  return data.filter(function(el) {
    return !!val.length ? objectValues(el, pick2).includes(strOp(val)) : true;
  });
}

// node_modules/react-filter-search/es/index.js
function _classCallCheck(instance, Constructor) {
  if (!(instance instanceof Constructor)) {
    throw new TypeError("Cannot call a class as a function");
  }
}
function _possibleConstructorReturn(self, call) {
  if (!self) {
    throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
  }
  return call && (typeof call === "object" || typeof call === "function") ? call : self;
}
function _inherits(subClass, superClass) {
  if (typeof superClass !== "function" && superClass !== null) {
    throw new TypeError("Super expression must either be null or a function, not " + typeof superClass);
  }
  subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } });
  if (superClass)
    Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass;
}
var FilterResults = function(_Component) {
  _inherits(FilterResults2, _Component);
  function FilterResults2() {
    _classCallCheck(this, FilterResults2);
    return _possibleConstructorReturn(this, _Component.apply(this, arguments));
  }
  FilterResults2.prototype.render = function render() {
    var _props = this.props, value = _props.value, data = _props.data, pick2 = _props.pick;
    return this.props.renderResults(filter(value, data, pick2));
  };
  return FilterResults2;
}(import_react.Component);
FilterResults.propTypes = true ? {
  value: import_prop_types.default.string.isRequired,
  data: import_prop_types.default.arrayOf(import_prop_types.default.object).isRequired,
  renderResults: import_prop_types.default.func.isRequired,
  pick: import_prop_types.default.arrayOf(import_prop_types.default.string)
} : {};
export {
  FilterResults as default
};
//# sourceMappingURL=react-filter-search.js.map
